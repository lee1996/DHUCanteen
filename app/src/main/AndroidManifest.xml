<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.customshoppingcardemo">

    <application
        android:allowBackup="true"
        android:icon="@drawable/eat"
        android:label="DHU热量"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"/>

        <activity android:name="com.example.welcome.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
       <activity android:name="com.example.person.PersonActivity"/>
        <activity android:name="com.example.person.UpdateActivity"/>
    </application>

</manifest>
