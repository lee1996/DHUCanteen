<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.customshoppingcardemo">

    <application
        android:allowBackup="true"
        android:icon="@drawable/eat"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"/>
        <activity android:name="com.example.welcome.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
       <activity android:name="com.example.person.PersonActivity" android:theme="@style/JK.SwipeBack.Transparent.Theme"
           />
        <activity android:name="com.example.person.UpdateActivity" android:theme="@style/JK.SwipeBack.Transparent.Theme"/>
        <activity android:name="com.example.person.LinkActivity" android:theme="@style/JK.SwipeBack.Transparent.Theme"/>
    </application>

</manifest>
